{% extends 'base.html.twig' %}

{% block title %}Delivry{% endblock %}

{% block body %}
    <h1>Delivry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ delivry.name }}</td>
            </tr>
            <tr>
                <th>Activity</th>
                <td>{{ delivry.activity }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ delivry.phone }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ delivry.createdAt ? delivry.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('delivry_index') }}">back to list</a>

    <a href="{{ path('delivry_edit', {'id': delivry.id}) }}">edit</a>

    {{ include('delivry/_delete_form.html.twig') }}
{% endblock %}
