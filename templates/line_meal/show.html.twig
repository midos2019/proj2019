{% extends 'base.html.twig' %}

{% block title %}LineMeal{% endblock %}

{% block body %}
    <h1>LineMeal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line_meal.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ line_meal.quantity }}</td>
            </tr>
            <tr>
                <th>Unit_price</th>
                <td>{{ line_meal.unitPrice }}</td>
            </tr>
            <tr>
                <th>Tax</th>
                <td>{{ line_meal.tax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('line_meal_index') }}">back to list</a>

    <a href="{{ path('line_meal_edit', {'id': line_meal.id}) }}">edit</a>

    {{ include('line_meal/_delete_form.html.twig') }}
{% endblock %}
