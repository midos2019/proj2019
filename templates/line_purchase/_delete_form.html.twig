<form method="post" action="{{ path('line_purchase_delete', {'id': line_purchase.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ line_purchase.id) }}">
    <button class="btn">Delete</button>
</form>
